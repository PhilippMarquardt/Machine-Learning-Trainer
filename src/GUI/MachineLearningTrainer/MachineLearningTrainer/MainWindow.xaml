<Window
    x:Class="MachineLearningTrainer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MachineLearningTrainer"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Annotation Tool"
    Width="800"
    Height="800"
    MinWidth="800"
    MinHeight="600"
    Background="{DynamicResource MaterialDesignPaper}"
    FontFamily="{DynamicResource MaterialDesignFont}"
    Foreground="White"
    Icon="/Icons/hsa.ico"
    ResizeMode="CanResizeWithGrip"
    ShowInTaskbar="True"
    WindowStartupLocation="CenterScreen"
    WindowState="Maximized"
    mc:Ignorable="d">
    <Grid>
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="0.05*"/>
            <RowDefinition Height="0.95*"/>
        </Grid.RowDefinitions>-->
        <Grid x:Name="MainGrid" Grid.Row="1">
        </Grid>
        <!--<WrapPanel Grid.Row="0" HorizontalAlignment="Right">
            <Button x:Name="btnMinimize" VerticalAlignment="Center" Background="Transparent" Margin="1" VerticalContentAlignment="Bottom" Click="btnMinimize_Click">
                <materialDesign:PackIcon Kind="WindowMinimize" Foreground="Black" VerticalContentAlignment="Bottom"/>
            </Button>
            <Button x:Name="btnMaximize" VerticalAlignment="Center" Background="Transparent" Margin="1" Click="btnMaximize_Click">
                <materialDesign:PackIcon Kind="WindowMaximize" Foreground="Black"/>
            </Button>
            <Button x:Name="btnClose" VerticalAlignment="Center" Background="Transparent" Margin="1" Click="btnClose_Click">
                <materialDesign:PackIcon Kind="WindowClose" Foreground="Black"/>
            </Button>
        </WrapPanel>-->



    </Grid>
</Window>
